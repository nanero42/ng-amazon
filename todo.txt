- link component
 -- link types: text, image
- button component:
 -- create it
 -- add cursor pointer to button component
- make css-variables avaliable in both js and scss
- toggle dark / light / any_other_color theme

- slider:
 -- extract slides array from slider component to home-page
 -- now slider works only for desktop (mouse event), add support for mobile (touch event)
 -- add ability to slide back from the first slide and return to the last one slide and vice versa.
- add truncate to deliver-to
- finish autocomplete
- add fonts
- add drawer component
- show drawer on burger click in header
